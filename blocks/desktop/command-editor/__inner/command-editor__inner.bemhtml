block('command-editor').elem('inner').content()(function() {
    return [
        {
            elem: 'title',
            content: this.ctx.title
        },
        {
            elem: 'card',
            content: {
                elem: 'close',
                tag: 'span',
                cls: 'fa fa-times fa-fw'
            }
        }
    ];
});
